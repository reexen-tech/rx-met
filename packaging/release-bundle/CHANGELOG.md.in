# rx-met @VERSION@ 版本说明

## 发布内容

- 发布制品包含 `cu118`、`cu126` 和 `cu130` 三个 Linux x86_64 GPU 镜像。
- 每个镜像包含对应版本的 Python、PyTorch、ONNX Runtime GPU、定制 AIMET 和
  QuantGRU。
- 发布目录包含镜像归档、`image-manifest.json`、`SHA256SUMS` 和与镜像一致的
  `examples/` 示例源码。

## 兼容性

- CUDA、Python、PyTorch、ONNX Runtime 和最低驱动组合见同目录 `README.md`。
- 镜像支持的 GPU 计算架构记录在 `image-manifest.json`。
- KWS 和 ONNX PTQ 完整流程需要 NVIDIA GPU；CPU 模式用于 ONNX PTQ 调试。

## 使用限制

- 模型、数据集和校准样本由用户单独准备。
- 发布验收需要在目标 GPU、驱动和数据环境中完成。
- Clean ONNX 与编译器 encodings 按同次运行结果配套使用。

## 可追溯性

- `image-manifest.json` 记录镜像名称、版本和构建信息。
- `SHA256SUMS` 校验发布目录中的镜像归档、元数据和示例文件。
- 源码仓库的 `CHANGELOG.md` 记录各版本之间的功能和兼容性变化。
