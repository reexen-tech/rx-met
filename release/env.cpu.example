# 用法：setup.sh 会复制为本机 workspace/.env，按路径改成你的实际值即可。
# 本文件用于 CPU 发布包（无 GPU / 无 QuantGRU）。

IMAGE=rx-met:@VERSION@-cpu
WORKSPACE=
ENABLE_GPUS=0

# 模型目录（→ 容器 /models）
MODELS_DIR=/data/models

# 数据集目录（→ 容器 /datasets）；不用评测可留空
DATASETS_DIR=/data/datasets
