# 用法：setup.sh 会复制为本机 workspace/.env，按路径改成你的实际值即可。

IMAGE=rx-met:@VERSION@
WORKSPACE=
# 1=需要 NVIDIA Container Toolkit；CPU 包请用 env.cpu.example（ENABLE_GPUS=0）
ENABLE_GPUS=1

# 模型目录（→ 容器 /models）
MODELS_DIR=/data/models

# 数据集目录（→ 容器 /datasets）；不用评测/小模型数据可留空
DATASETS_DIR=/data/datasets
