# 用法：setup.sh 会复制为本机 workspace/.env，按下面三项改成你的实际路径即可。

IMAGE=rx-met:@VERSION@
WORKSPACE=

# 模型目录（→ 容器 /models），例如 /data/models/Qwen3.5-35B-A3B
MODELS_DIR=/data/models

# 数据集目录（→ 容器 /datasets），例如评测或训练数据集
DATASETS_DIR=/data/datasets
